[build-system]
requires = ['setuptools>=42.0','wheel']
build-backend = 'setuptools.build_meta'

[tools.pytest.ini_option]
testpaths=[
    'tests'
    ]
[tool.mypy]
mypy_path='src'
ignore_missing_imports = true